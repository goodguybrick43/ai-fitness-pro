name: Build APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EAS_NO_VARS_TOKEN }}

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Install dependencies
        run: npm install

      - name: Force Credential Generation
        # This command forces Expo to create the Keystore for your 'preview' profile
        run: eas build --platform android --profile preview --non-interactive --no-wait

      - name: Final Build Attempt
        # Now that the credentials were triggered in the step above, this should proceed
        run: eas build --platform android --profile preview --non-interactive
